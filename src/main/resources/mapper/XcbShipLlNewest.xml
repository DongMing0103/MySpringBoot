<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.XcbShipLlNewestMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.demo.entity.po.XcbShipLlNewest">
        <id column="id" property="id" />
        <result column="ship_num" property="shipNum" />
        <result column="ais_number" property="aisNumber" />
        <result column="mmsi_number" property="mmsiNumber" />
        <result column="longitude" property="longitude" />
        <result column="latitude" property="latitude" />
        <result column="pro_name" property="proName" />
        <result column="pro_code" property="proCode" />
        <result column="city_name" property="cityName" />
        <result column="city_code" property="cityCode" />
        <result column="area_name" property="areaName" />
        <result column="area_code" property="areaCode" />
        <result column="newest_time" property="newestTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, ship_num, ais_number, mmsi_number, longitude, latitude, pro_name, pro_code, city_name, city_code, area_name, area_code, newest_time
    </sql>

</mapper>
